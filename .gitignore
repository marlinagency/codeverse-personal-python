# secrets — NEVER commit real keys; .env.example is the committed template
.env

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# node
frontend/node_modules/
frontend/dist/

# local data / scratch
codeverse.db
.tmp/
# amd/codeverse_theme_sft.jsonl is committed on purpose — the real,
# Fireworks-generated distillation dataset used for the AMD fine-tune, kept
# as verifiable hackathon evidence. Only the tiny pilot run stays ignored.
amd/pilot_sft.jsonl

# editors / os
.vscode/
.idea/
Thumbs.db
.DS_Store

# build artifacts
backend/src/codeverse.egg-info/
