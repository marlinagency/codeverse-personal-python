"""CodeVerse core engine: theme mapping, DSL front-end, UASL, and codegen.

This package is pure domain logic. It has no dependency on FastAPI, the
database layer, or Docker — those live in ``codeverse_api`` and
``codeverse_sandbox`` respectively.
"""

__version__ = "0.1.0"
